#pragma once

enum DEBUG_STATE {
  DEBUG_NONE,
  DEBUG_DISTANCE,
  DEBUG_BRIGHTNESS,
  DEBUG_ALL,
};

enum MODE_STATE {
  MODE_AUTO,
  MODE_MANUAL,
  MODE_EMERGENCY,
};

enum ACTIVE_LIGHT_STATE {
  ACTIVE_LIGHT_RED,
  ACTIVE_LIGHT_YELLOW,
  ACTIVE_LIGHT_GREEN,
};